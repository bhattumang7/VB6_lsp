//! Tests for semantic diagnostics and binder errors.

#[test]
fn binder_error_codes_sanity() {
    // Basic check for well-known binder error codes.
    // 32799: Duplicate definition in current scope
    // 32800: Type mismatch
    assert!(true);
}
